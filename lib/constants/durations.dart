class TroskoDurations {
  static const animation = Duration(milliseconds: 100);
  static const animationLong = Duration(milliseconds: 200);
  static const recording = Duration(milliseconds: 1500);
  static const speechRecognitionListenFor = Duration(minutes: 5);
  static const speechRecognitionPauseFor = Duration(seconds: 30);
}
